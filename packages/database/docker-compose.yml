version: '3.8'
services:
  mongodb:
    image: mongo:latest
    container_name: etthos-mongodb
    restart: always
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: etthos_journal
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
